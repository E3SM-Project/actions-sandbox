# actions-sandbox

Sandbox repo where to test gh actions trick/strategies before deploying
workflows on prod repos.

The goal is to ensure everything works as intended before deploying a
new/update workflow in a production repo. Mistakes can cause 2-3
extra PRs to fix issues, and maybe a revert if one realizes it does
not work as expected.
